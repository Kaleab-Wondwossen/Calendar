import 'package:flutter/material.dart';

class MyEventContainer extends StatelessWidget {
  const MyEventContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}